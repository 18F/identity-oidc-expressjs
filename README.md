# my-identity-sp

An example service provider to authenticate users with login.gov using OIDC.

## Prerequisites

Generate keys:

```sh
npm run keygen
```

## Usage

Run a local webserver:

```sh
npm run start
```
